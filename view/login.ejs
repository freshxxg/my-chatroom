<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="content-type" content="text/html; charset=UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0, maximum-scale=1.0">
    <meta name="applicable-device" content="pc,mobile">
    <title>Chatroom</title>
    <link rel='stylesheet' href='/bootstrap.min.css'/>
    <link rel="stylesheet" href="/login.css">
    <script src='/jquery.min.js'></script>
    <script src="/bootstrap.min.js"></script>
</head>
<body>
<div class="container">
    <div class="form-signin">
        <h2 class="form-signin-heading">管理员登录系统</h2>
        <input type="text" name="username"  class="admin" placeholder="账号">
        <input type="password" name="password" class="password" placeholder="密码">
        <p><button class="btn btn-large btn-primary" type="submit" onclick="ajaxsubmit();">登录</button></p>
    </div>
</div>
<script>
    function ajaxsubmit() {
        var admin = $(".admin").val();
        var password = $(".password").val();
        if(admin === "范晶" && password ==="admin"){
            location.href = '/admin';
        }else if (admin == "" || admin == null) {
            alert("账户名不能为空");
        } else if (password == "" || password == null) {
            alert("密码不能为空");
        } else {
            alert("账户名或密码错误");
        }
    }
</script>
</body>
</html>